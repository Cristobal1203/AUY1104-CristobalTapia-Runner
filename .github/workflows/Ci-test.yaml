name: CI - Test Pipeline
 
on:
  push:
    branches-ignore:
      - main
    tags-ignore:
      - '**'
 
jobs:
  run-tests:
    uses: Cristobal1203/AUY1104-CristobalTapia-Main/.github/workflows/holamundo.yaml@main
    # Hola 